# Hard case infobox check

The live page `The Lord of the Rings - Original Motion Picture Soundtrack` uses the named `album` template and revision 433235. Its title-cased variant is a real redirect to that page. Both revisions were fetched through the headed browser session on server2 and stored as separate corpus pages.
